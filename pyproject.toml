[project]
name = "SystemSmith-docs"
version = "0.1.0"
description = "README generation utilities for SystemSmith."
requires-python = ">=3.11"
dependencies = [
    "rich>=13.7,<14",
    "toc-markdown>=0.1.0,<0.2",
]

[tool.uv]
package = false
